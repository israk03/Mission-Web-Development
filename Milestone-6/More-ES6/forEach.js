/**
 * syntax: 
        array.forEach((element, index, array) => {
            // Code to execute for each element
        });

    kind of map(), but doesn't return anything
 */

const num = [1, 2, 3, 4, 5];
num.forEach(n => console.log(n * 2));